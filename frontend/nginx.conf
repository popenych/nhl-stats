server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback — client-side routing (React Router) owns unknown paths.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
